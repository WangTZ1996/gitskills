function functionName ( arr ){
	var p;
	for ( var i=1 ; i < arr.length ; i++ ){
		for ( var j=i-1; j >= 0; j--){
 			if( arr[j+1]<arr[j]){
				p = arr[j+1];
				arr[j+1] = arr[j];
				arr[j] = p;
			}else if (arr[j+1]>=arr[j]){
				break;
				}	
		}
	}
	return arr;

}

//插入排序

